import { HighlightTagPipe } from './highlight-tag.pipe';

describe('HighlightTagPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightTagPipe();
    expect(pipe).toBeTruthy();
  });
});
