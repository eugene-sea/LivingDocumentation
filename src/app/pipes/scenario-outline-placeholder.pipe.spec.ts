import { ScenarioOutlinePlaceholderPipe } from './scenario-outline-placeholder.pipe';

describe('ScenarioOutlinePlaceholderPipe', () => {
  it('create an instance', () => {
    const pipe = new ScenarioOutlinePlaceholderPipe();
    expect(pipe).toBeTruthy();
  });
});
